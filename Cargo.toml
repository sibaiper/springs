[package]
name = "springs"
version = "0.1.1"
edition = "2024"

[dependencies]

# Only the `visual` example needs a window; the library itself stays dependency free.
[dev-dependencies]
minifb = "0.28"
